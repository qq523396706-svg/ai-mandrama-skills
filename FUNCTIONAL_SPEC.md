# AI 漫剧 Skills 独立功能规格

版本：1.0

状态：重写输入基线

## 产品目标

为支持 Agent Skills 的运行环境提供一组可组合的 AI 剧情视频生产能力。系统应把创作决定、生成执行、费用授权和成片验收分开，使长篇或多集项目能够恢复、追踪和局部返修。

## 六项能力

### 1. `ai-mandrama-production`

负责识别项目目前处于哪个生产阶段，确认上一步的权威输入，选择专项能力，并记录变更对下游交付物的影响。它不替代编剧、表演、声音、生成或质检专家直接完成其专业工作。

### 2. `emotion-performance-director`

把人物在场景中的目标、掩饰、刺激和选择转换成可观察的眼神、呼吸、面部控制、姿态与动作变化。不得擅自改变台词、人物关系、道具结果、镜头数量或摄影机任务。

### 3. `mandrama-voice-director`

维护固定角色可辨识的声音设定，并为每句已批准台词指定对象、说话策略、语速、重音、停顿、气息和目标时长。不得无授权克隆真人声音或静默改词。

### 4. `mandrama-lipsync-qc`

在生成前评估画内对白的同步难度，在成片后逐镜播放声音和画面，定位发声者错误、开合口时点、音素可读性、静默张嘴和嘴部伪影，并把返修送回最小责任层。

### 5. `seedance-batch-ops`

管理多镜视频任务的输入版本、预算、授权、平台回执、状态、有限重试、下载文件和技术检查。它接收已经批准的提示词，不负责创作或偷偷修改提示词。

### 6. `mandrama-final-qc`

对最终候选文件进行完整播放和多维验收，输出带时间点的问题、严重度、责任层和复验范围。只有完成必要观看且不存在阻断性交付问题时才能签发通过。

## 共同生产模型

全链路使用九个工作区，而不是把模型调用当作起点：

1. `SCOPE`：用途、受众、素材权利、预算和发布边界；
2. `STORY`：系列规则、人物驱动力和分集承诺；
3. `SCRIPT`：可见、可听、可表演的单集文本；
4. `DESIGN`：角色、场景、道具和声音身份；
5. `PLAN`：场面调度、镜头职责和剪辑交接；
6. `TIMING`：故事板进入真实时长后的对白与动作预演；
7. `GENERATE`：经授权的图片、音频和视频任务；
8. `ASSEMBLE`：剪辑、声音、字幕和图形；
9. `VERIFY`：最终文件的内容、技术、权利与发布检查。

关键批准点：

- `STORY_ACCEPTED`：系列方向可以进入单集文本；
- `SCRIPT_ACCEPTED`：指定剧本版本可以驱动正式设计；
- `DESIGN_ACCEPTED`：身份与空间基准可以驱动镜头计划；
- `TIMING_ACCEPTED`：实际时长预演允许进入正式生成；
- `GENERATION_ALLOWED`：费用、上传、肖像与声音权利得到覆盖；
- `DELIVERY_ACCEPTED`：最终文件完成播放验收。

批准必须绑定具体版本和批准者。上游发生实质变化时，只使实际依赖它的下游结果失效。

## 通用数据合同

每个阶段至少记录：

- 当前工作区和输入版本；
- 已确认事实、假设和未知项；
- 本次负责的 Skill 或工具；
- 新产生或更新的交付物；
- 外部副作用及授权状态；
- 验证证据；
- 结论：`READY / NEEDS_WORK / WAITING / NOT_CHECKED`；
- 下一步和返修入口。

## 外部依赖边界

- 故事开发、单集写作和独立审稿可以路由给目标环境中已安装的专业写作 Skill；
- 视频提示词可以路由给已安装的 Seedance 提示词 Skill；
- 图片、配音、视频平台和剪辑工具由使用者选择；
- 名称引用不代表打包、安装、授权或认可；
- 上传、付费、真人肖像、声音克隆和公开发布均需明确授权。

## 成功标准

- 项目可以从磁盘交付物恢复，而不依赖聊天记忆；
- 每个正式生成结果能追溯到批准输入和平台回执；
- 失败有分类、有修改变量、有尝试上限；
- 表演、声音和口型分别验收；
- 技术检查不冒充创意验收；
- 最终状态由真实文件完整播放决定；
- 没有授权时系统会停止，而不是替用户补充权限。

## 明确排除

- 一句话直接批量生成整季；
- 在剧本未批准前制作正式角色或项目视频；
- 自动安装外部 Skill、上传素材或调用付费接口；
- 使用未授权真人肖像或声音；
- 用任务成功、缩略图、抽帧或文件存在代替成片验收；
- 将第三方资料、客户项目或本机路径写进发布包。
