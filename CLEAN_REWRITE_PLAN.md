# 隔离重写计划

状态：重写完成，等待公开权利复核

建立日期：2026-09-08

## 目标

从空白 Skill 脚手架重新实现六项 AI 漫剧能力，保留用户需要的生产效果与安全门，不复用旧 Skill 或第三方 Skill 的原句、段落、表格、案例、故障码和文件布局。

本项目属于“隔离重写”，不宣称为法律意义上的严格 clean-room：执行者曾经阅读过旧版本。最终能否公开授权仍需来源审查和用户确认。

## 写入边界

唯一允许写入的位置是本隔离重写仓库根目录及其子目录：

```text
<isolated-rewrite-repository>
```

以下位置保持只读：

- 正在使用的本机 Skill；
- 先前的审计副本及其 Git 历史；
- 本地漫剧项目、品牌项目、客户资料和生成素材；
- 任何第三方资料包或仓库副本。

## 来源纪律

1. 重写阶段只使用本目录的 `FUNCTIONAL_SPEC.md` 作为内容需求，不从旧 Skill 复制文字。
2. 保留通用能力名称和必要接口名，但更换章节体系、状态词、模板字段与诊断编码。
3. 如果某项能力只能通过复制第三方表达实现，标记 `RIGHTS_BLOCKED`，不写入发布版。
4. 外部 Skill 只作为可选依赖按名称引用，不打包其正文、模板、代码或资产。
5. 相似度工具只做风险筛查，不把百分比当作法律结论。

## 实施阶段

1. 冻结功能规格和不可越权边界。
2. 使用官方 `skill-creator` 初始化六个空白 Skill。
3. 先写五个边界清晰的专项 Skill。
4. 再写总控 Skill，使其只协调交付物和检查点。
5. 编写全新公共 README、来源说明和暂缓许可声明。
6. 验证结构、链接、路由、编码和示例行为。
7. 对旧版与已知来源做逐文件相似度筛查，并人工检查长匹配。
8. 形成发布判定：`PRIVATE_READY / PUBLIC_REVIEW / RIGHTS_BLOCKED`。

## 完成标准

- 六个 Skill 均从空白脚手架生成，frontmatter 名称与目录一致；
- 总控能够路由另外五个 Skill，且每个专项 Skill 的责任不重叠；
- 不含本机绝对路径、个人或客户数据、凭据、媒体和项目脚本；
- 官方 `quick_validate.py` 六项通过；
- 所有相对 Markdown 链接存在；
- 相似度报告列出算法、阈值、命中位置和人工结论；
- 未完成权利确认前不添加 MIT，不创建远端、不 push、不设为 public。

## 回滚

重写版是全新目录。失败时删除或废弃本目录即可；原始 Skill 和当前审计副本不受影响。任何删除仍需用户明确授权。
